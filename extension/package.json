{
  "name": "dark-pattern-extension",
  "version": "1.0.0",
  "description": "Chrome extension for Dark Pattern Detector",
  "scripts": {
    "build": "echo 'Extension uses vanilla JS — no build step needed'"
  }
}
